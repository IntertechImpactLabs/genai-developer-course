Your task is to complete the implementation of a Mock Data MCP server.

Server code is located in 05_mcp_server/index.js

- Add the generate_users tool to the MCP server
- Implement a generator function for realistic user data
- Use the loaded data from the JSON files in loadMockData()
- Add arguments for optionally including
  address (include: true/false, default: false)
  company (include: true/false, default: false)
  address country (option: U.S. only or international, default: U.S. only)
- Create utility and generation functions in a separate file.
