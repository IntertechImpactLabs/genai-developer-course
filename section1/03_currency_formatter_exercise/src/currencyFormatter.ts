/**
 * Currency Formatter Exercise
 * 
 * TODO: Implement a formatCurrency function that:
 * 1. Accepts an amount (number) and currency code (string)
 * 2. Returns a properly formatted currency string
 * 3. Supports USD, EUR, and GBP at minimum
 * 4. Handles edge cases appropriately
 * 5. Uses the Intl.NumberFormat API for formatting
 */

// TODO: Define a type for supported currency codes
// type SupportedCurrency = ...

/**
 * Formats a numeric amount as a currency string
 * 
 * @param amount - The numeric amount to format
 * @param currencyCode - The ISO 4217 currency code (e.g., 'USD', 'EUR', 'GBP')
 * @returns The formatted currency string
 * @throws Error if the amount or currency code is invalid
 */
export function formatCurrency(amount: number, currencyCode: string): string {
  // TODO: Implement the formatCurrency function
  // 
  // Remember to:
  // 1. Validate the input parameters
  // 2. Check if the currency code is supported
  // 3. Use Intl.NumberFormat for proper localization
  // 4. Handle errors appropriately
  
  // Stub implementation - returns empty string
  // This ensures error handling tests don't pass accidentally
  return '';
}

// Optional: Add any helper functions or additional exports below