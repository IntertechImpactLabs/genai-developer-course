Generate an API for managing AI agent prompts using the .NET Core minimal API pattern in the prompt_api project.

- Support CRUD operations
- Use REST-ful endpoints with proper verbs
- Prompts should include
    - title
    - version
    - summary
    - text 
    - parameters: include name (replacement token), default value, and description
- Do not include additional libraries
- Do not include OpenApi
